* HelixFold

Accurate protein structure prediction can significantly accelerate the development of life science. The accuracy of AlphaFold2, a frontier end-to-end structure prediction system, is already close to that of the experimental determination techniques. Due to the complex model architecture and large memory consumption, it requires lots of computational resources and time to implement the training and inference of AlphaFold2 from scratch. The cost of running the original AlphaFold2 is expensive for most individuals and institutions. Therefore, reducing this cost could accelerate the development of life science. We implement AlphaFold2 using PaddlePaddle, namely HelixFold, to improve training and inference speed and reduce memory consumption. The performance is improved by operator fusion, tensor fusion, and hybrid parallelism computation, while the memory is optimized through Recompute, BFloat16, and memory read/write in-place. Compared with the original AlphaFold2 (implemented with Jax) and OpenFold (implemented with PyTorch), HelixFold needs only 7.5 days to complete the full end-to-end training and only 5.3 days when using hybrid parallelism, while both AlphaFold2 and OpenFold take about 11 days. HelixFold saves 1x training time. We verified that HelixFold's accuracy could be on par with AlphaFold2 on the CASP14 and CAMEO datasets. HelixFold's code is available on GitHub for free download: this https URL, and we also provide stable web services on this https URL.

> 精准的蛋白质结构预测能够极大推动生命科学的发展。前沿端到端结构预测系统AlphaFold2的预测精度已经接近实验测定技术。但因其模型架构复杂、内存开销大，从零完成AlphaFold2的训练与推理需要消耗大量计算资源与时间。对于绝大多数个人和科研机构而言，运行原版AlphaFold2成本高昂。因此，降低该计算成本将有助于生命科学的进一步发展。本文基于飞桨（PaddlePaddle）实现了AlphaFold2，命名为HelixFold，旨在提升训练与推理速度、降低内存占用。该工作通过算子融合、张量融合以及混合并行计算提升模型性能；同时借助重计算（Recompute）、BFloat16精度、内存原地读写完成内存优化。与基于Jax实现的原版AlphaFold2、基于PyTorch实现的OpenFold相比，HelixFold完成完整端到端训练仅需7.5天，启用混合并行模式后仅需5.3天，而AlphaFold2与OpenFold均需要约11天，HelixFold将训练时长缩短一倍。我们在CASP14与CAMEO数据集上验证，HelixFold的预测精度可以与AlphaFold2持平。HelixFold的代码可在GitHub免费下载：该https网址，同时我们也提供稳定的网页服务：
该https网址。


* HelixFold-Single

AI-based protein structure prediction pipelines, such as AlphaFold2, have achieved near-experimental accuracy. These advanced pipelines mainly rely on Multiple Sequence Alignments (MSAs) as inputs to learn the co-evolution information from the homologous sequences. Nonetheless, searching MSAs from protein databases is time-consuming, usually taking dozens of minutes. Consequently, we attempt to explore the limits of fast protein structure prediction by using only primary sequences of proteins. HelixFold-Single is proposed to combine a large-scale protein language model with the superior geometric learning capability of AlphaFold2. Our proposed method, HelixFold-Single, first pre-trains a large-scale protein language model (PLM) with thousands of millions of primary sequences utilizing the self-supervised learning paradigm, which will be used as an alternative to MSAs for learning the co-evolution information. Then, by combining the pre-trained PLM and the essential components of AlphaFold2, we obtain an end-to-end differentiable model to predict the 3D coordinates of atoms from only the primary sequence. HelixFold-Single is validated in datasets CASP14 and CAMEO, achieving competitive accuracy with the MSA-based methods on the targets with large homologous families. Furthermore, HelixFold-Single consumes much less time than the mainstream pipelines for protein structure prediction, demonstrating its potential in tasks requiring many predictions. The code of HelixFold-Single is available at this https URL, and we also provide stable web services on this https URL.

> 的基于人工智能的蛋白质结构预测流程已经达到接近实验测定的精度。这类先进的预测流程主要以多序列比对（MSA）作为输入，从同源序列中学习共进化信息。然而，从蛋白质数据库中检索多序列比对结果十分耗时，通常需要数十分钟。为此，我们尝试仅利用蛋白质的一级序列，探究快速蛋白质结构预测的性能上限。本文提出 HelixFold‑Single，将大规模蛋白质语言模型与 AlphaFold2 出色的几何学习能力相结合。该方法首先采用自监督学习范式，使用数十亿条蛋白质一级序列对大规模蛋白质语言模型（PLM）进行预训练，以此替代多序列比对来获取共进化信息。随后将预训练的蛋白质语言模型与 AlphaFold2 的核心模块相结合，得到端到端可微模型，仅依靠一级序列即可预测原子的三维坐标。在 CASP14 与 CAMEO 数据集上对 HelixFold‑Single 开展验证，对于同源家族规模较大的目标蛋白，该模型取得了与基于多序列比对的方法相媲美的预测精度。此外，HelixFold‑Single 的运算耗时远低于主流蛋白质结构预测流程，在大批量预测任务中具备应用潜力。HelixFold‑Single 的代码可在此 https 链接获取，我们同时也提供稳定的网页服务，访问地址为此 https 链接。

* HelixFold-Multimer 

While monomer protein structure prediction tools boast impressive accuracy, the prediction of protein complex structures remains a daunting challenge in the field. This challenge is particularly pronounced in scenarios involving complexes with protein chains from different species, such as antigen-antibody interactions, where accuracy often falls short. Limited by the accuracy of complex prediction, tasks based on precise protein-protein interaction analysis also face obstacles. In this report, we highlight the ongoing advancements of our protein complex structure prediction model, HelixFold-Multimer, underscoring its enhanced performance. HelixFold-Multimer provides precise predictions for diverse protein complex structures, especially in therapeutic protein interactions. Notably, HelixFold-Multimer achieves remarkable success in antigen-antibody and peptide-protein structure prediction, greatly surpassing AlphaFold 3. HelixFold-Multimer is now available for public use on the PaddleHelix platform, offering both a general version and an antigen-antibody version. Researchers can conveniently access and utilize this service for their development needs.

> 尽管单体蛋白质结构预测工具已具备出色的预测精度，但蛋白质复合物结构预测仍是该领域的一大严峻挑战。在包含来自不同物种蛋白链的复合物场景下，例如抗原‑抗体相互作用，这一挑战表现得尤为突出，预测精度往往不尽如人意。受限于复合物预测的精度，依赖精准蛋白质‑蛋白质相互作用分析的相关任务同样遭遇阻碍。本报告介绍了我们的蛋白质复合物结构预测模型HelixFold‑Multimer取得的最新进展，着重说明其性能提升。HelixFold‑Multimer能够对多种蛋白质复合物结构实现精准预测，尤其适用于治疗性蛋白的相互作用场景。值得关注的是，HelixFold‑Multimer在抗原‑抗体以及多肽‑蛋白质结构预测上取得优异效果，性能大幅超越AlphaFold 3。目前HelixFold‑Multimer已在PaddleHelix平台对外开放，提供通用版本与抗原‑抗体版本，科研人员可便捷调用该服务以满足自身研究开发需求。




* HelixFold3

The AlphaFold series has transformed protein structure prediction with remarkable accuracy, often matching experimental methods. AlphaFold2, AlphaFold-Multimer, and the latest AlphaFold3 represent significant strides in predicting single protein chains, protein complexes, and biomolecular structures. While AlphaFold2 and AlphaFold-Multimer are open-sourced, facilitating rapid and reliable predictions, AlphaFold3 remains partially accessible through a limited online server and has not been open-sourced, restricting further development. To address these challenges, the PaddleHelix team is developing HelixFold3, aiming to replicate AlphaFold3's capabilities. Leveraging insights from previous models and extensive datasets, HelixFold3 achieves accuracy comparable to AlphaFold3 in predicting the structures of the conventional ligands, nucleic acids, and proteins. The initial release of HelixFold3 is available as open source on GitHub for academic research, promising to advance biomolecular research and accelerate discoveries. The latest version will be continuously updated on the HelixFold3 web server, providing both interactive visualization and API access.

> AlphaFold系列模型凭借极高的精度革新了蛋白质结构预测，其预测结果往往与实验方法结果相当。AlphaFold2、AlphaFold‑Multimer以及最新的AlphaFold3在单条蛋白链、蛋白质复合物以及生物分子结构的预测方面取得了重大进展。AlphaFold2与AlphaFold‑Multimer已经开源，可实现快速可靠的预测；而AlphaFold3仅能通过受限的在线服务器部分使用，并未开源，制约了后续的技术发展。为解决上述难题，飞桨螺旋（PaddleHelix）团队研发了HelixFold3，目标是复刻AlphaFold3的能力。该模型借鉴已有模型的研究成果并依托海量数据集，在常规配体、核酸以及蛋白质的结构预测上实现了与AlphaFold3相当的精度。HelixFold3首个版本已在GitHub开源，面向学术研究使用，有望推动生物分子研究发展、加速科研成果产出。最新版本会在HelixFold3网页服务器持续更新，同时提供交互式可视化功能与API调用接口。